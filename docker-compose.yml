civ-historian:
    build: .
    ports:
      - "8006:8000"
    volumes:
      - ./sessions:/app/sessions
      - $HOME/.claude:/app/.claude:ro
    env_file:
      - ./.env
    restart: unless-stopped
